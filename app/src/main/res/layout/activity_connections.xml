<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_window_background"
    tools:context=".ConnectionsActivity">

    <!-- 2D Infinite Graph View Filling Screen -->
    <com.example.apptempleate.DagGraph2DView
        android:id="@+id/dagGraph2DView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Chat-style translucent fade, applied only to the top of the graph. -->
    <View
        android:layout_width="0dp"
        android:layout_height="96dp"
        android:background="@drawable/bg_top_chat_gradient"
        android:clickable="false"
        android:elevation="6dp"
        android:focusable="false"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Top Overlay Header Row -->
    <RelativeLayout
        android:id="@+id/headerOverlay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingTop="16dp"
        android:paddingBottom="12dp"
        android:background="@android:color/transparent"
        android:elevation="8dp"
        app:layout_constraintTop_toTopOf="parent">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:background="@drawable/bg_circle_icon_button"
            android:contentDescription="Back"
            android:padding="9dp"
            android:src="@drawable/ic_close"
            app:tint="@color/text_primary" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:gravity="center"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvConnectionsTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="2D DAG Knowledge Plane"
                android:textColor="@color/text_primary"
                android:textSize="17sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvConnectionsCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="0 Active Non-Zero Connections"
                android:textColor="@color/accent_blue"
                android:textSize="12sp"
                android:textStyle="bold" />

        </LinearLayout>

        <ImageButton
            android:id="@+id/btnResetView"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="@drawable/bg_circle_icon_button"
            android:contentDescription="Reset Graph Position"
            android:padding="10dp"
            android:src="@drawable/ic_insights"
            app:tint="@color/text_primary" />

    </RelativeLayout>

    <!-- Floating Zoom Controls (Bottom Right) -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="90dp"
        android:elevation="6dp"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageButton
            android:id="@+id/btnZoomIn"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:background="@drawable/bg_circle_icon_button"
            android:contentDescription="Zoom In"
            android:padding="10dp"
            android:src="@drawable/ic_plus"
            app:tint="@color/text_primary" />

        <ImageButton
            android:id="@+id/btnZoomOut"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:layout_marginTop="10dp"
            android:background="@drawable/bg_circle_icon_button"
            android:contentDescription="Zoom Out"
            android:padding="10dp"
            android:src="@drawable/ic_trash"
            app:tint="@color/text_primary" />

    </LinearLayout>

    <!-- Bottom Help Pill Overlay -->
    <TextView
        android:id="@+id/tvGraphHelpHint"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:layout_marginBottom="20dp"
        android:background="@drawable/bg_floating_chat_box"
        android:elevation="4dp"
        android:gravity="center"
        android:paddingHorizontal="16dp"
        android:paddingVertical="10dp"
        android:text="2D Infinite Plane • Drag to Pan • Pinch to Zoom • Tap Dot for Connections"
        android:textColor="@color/text_tertiary"
        android:textSize="12sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
