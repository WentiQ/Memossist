<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apptempleate">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:label="Memossist"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppTempeLate">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppTempeLate">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".VoiceConversationActivity"
            android:exported="false"
            android:theme="@style/Theme.AppTempeLate" />

        <activity
            android:name=".MemoryVaultActivity"
            android:exported="false"
            android:theme="@style/Theme.AppTempeLate" />

        <activity
            android:name=".CognitiveInsightsActivity"
            android:exported="false"
            android:theme="@style/Theme.AppTempeLate" />

        <activity
            android:name=".ConnectionsActivity"
            android:exported="false"
            android:theme="@style/Theme.AppTempeLate" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.AppTempeLate" />

    </application>

</manifest>
